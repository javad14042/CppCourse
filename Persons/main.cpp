#include <iostream>
#include "person.h"
#include "customer.h"
#include "admin.h"

int main() {

    //reusable code
    //avoid duplication
    //encapsulation
    //reusable object -> write common attributes one time

    //functions
    //OOP -> class / object

    person ali;
    ali.SetName("Ali");

    customer hasan;
    hasan.SetName("Hasan");
    hasan.getName();
    hasan.balance

    admin amir;
    amir.getCustomerList();
    return 0;
}
